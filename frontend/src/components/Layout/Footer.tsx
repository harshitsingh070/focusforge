import React from 'react';

const Footer: React.FC = () => (
  <footer className="mt-8 border-t border-white/50 py-6">
    <div className="mx-auto max-w-7xl px-4 text-center text-xs text-ink-muted sm:px-6 lg:px-8">
      FocusForge consistency engine. Build habits one day at a time.
    </div>
  </footer>
);

export default Footer;
